package com.istech.buscourse

import androidx.appcompat.app.AppCompatActivity

/**
 * エントリポイント（フェーズ0時点は空）。
 * 権限リクエストと「運行開始」導線（設計書§4.3: FGS 起動は必ずフォアグラウンド操作を起点にする）を後続フェーズで実装する。
 */
class MainActivity : AppCompatActivity()
